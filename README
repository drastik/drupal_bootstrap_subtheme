The script is very basic, for my own needs.  

Run it from your Drupal webroot.  
It makes a few assumptions:
This script assumes these paths exist:  
- themes/contrib/bootstrap/starterkits/cdn|sass|less
- themes/custom/your_theme_name (will be created)

